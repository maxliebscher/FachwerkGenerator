# Render module

The v0.7 parity build currently boots the mechanically extracted canvas renderer from `src/app/legacy-runtime.ts`. Future feature branches should move individual drawing families into this folder behind tests instead of editing the extracted runtime directly.
